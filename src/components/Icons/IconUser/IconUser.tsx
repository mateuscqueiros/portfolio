import { IconUser as TablerIconUser } from "@tabler/icons-react";
import { IconProps } from "../icon.types";

export function IconUser(props: IconProps) {
  return <TablerIconUser {...props} className="text-inherit" />;
}
